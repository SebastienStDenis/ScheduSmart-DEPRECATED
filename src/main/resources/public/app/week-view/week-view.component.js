angular.
	module('weekView').
	component('weekView', {
		templateUrl: 'app/week-view/week-view.template.html',
		controller: ['Schedules', // remove Schedules from here ?
			function WeekViewController(Schedules) { // remove controller field?
			
		}
	]
});